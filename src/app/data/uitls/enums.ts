export enum LoginEnum {
  login,
  signup,
  privacy,
}
export enum ProfileEnum {
  profile = 'Profile',
  trips = 'Trips',
  security = 'Security',
}

export enum ICONS {
  mode_edit = 'mode_edit',
  profile = 'person',
  phone = 'call',
  mail = 'mail',
  calendar = 'date_range',
  country = 'flag',
  title = 'title',
  pass = 'lock',
  ticket = 'confirmation_number',
}
