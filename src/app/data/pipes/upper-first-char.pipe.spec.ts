import { UpperFirstCharPipe } from './upper-first-char.pipe';

describe('UpperFirstCharPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperFirstCharPipe();
    expect(pipe).toBeTruthy();
  });
});
