import { PriceCaculatorPipe } from './price-caculator.pipe';

describe('PriceCaculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceCaculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
