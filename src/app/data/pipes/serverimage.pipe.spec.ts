import { ServerimagePipe } from './serverimage.pipe';

describe('ServerimagePipe', () => {
  it('create an instance', () => {
    const pipe = new ServerimagePipe();
    expect(pipe).toBeTruthy();
  });
});
