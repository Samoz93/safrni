import { SocialLinkDirective } from './social-link.directive';

describe('SocialLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialLinkDirective();
    expect(directive).toBeTruthy();
  });
});
